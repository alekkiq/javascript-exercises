'use strict';

const form = document.querySelector('#form');
const apiUrl = form.action;

form.addEventListener('submit', (e) => {
    // whats even the point of this assignment exactly
    console.log(e)
});